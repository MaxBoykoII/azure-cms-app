version: '3.8'

services:
  cms:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
    ports:
      - 5004:5000
    environment: 
      - FLASK_ENV=development
